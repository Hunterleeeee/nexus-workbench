# 可选依赖：功能在 import 时按需降级，缺失不会导致启动失败。
#
# 核心 requirements.txt 不含重依赖；需要对应功能时再安装本文件。
# 安装失败只打日志、不阻断发布（对应功能自动降级）。
#   crawl4ai      -> /crawl4ai 网页抓取与研究（开源版默认禁用入口，按需启用）
#   pywebpush     -> 浏览器 Web Push 推送
# 注：cryptography（VAPID 签名）在核心 requirements.txt / requirements.lock 中，无需在此安装。
crawl4ai>=0.9.2
pywebpush>=2.0.0
